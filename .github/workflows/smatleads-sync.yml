name: smatleads-sync (GBP/Google Maps ingest)
# Calls the smatleads.io API to search Google Business Profile places for each combo
# in config/smatleads-matrix.json (2,168 entries). Runs 333 searches per day by default.
# Budget: 10,000 total credits — 333/day = ~30-day runway (monthly reset). Wave-5 target.
on:
  schedule:
    - cron: "0 6 * * *"   # 06:00 UTC daily (before engine-cycle picks up new leads at 07:00)
  workflow_dispatch:
    inputs:
      max_searches:
        description: "Max searches this run (default 333, ~10k/mo budget)"
        required: false
        default: "333"
      force:
        description: "Skip city-today dedup check (re-run already-searched combos)"
        required: false
        default: "false"
      dry:
        description: "Dry run — search but do not write to Neon"
        required: false
        default: "false"

# Single-flight: two concurrent runs would double-consume the credit budget.
concurrency:
  group: smatleads-sync
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: "24" }
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with: { python-version: "3.12" }
      - run: npm install --omit=dev || npm install || true
      - run: python -m pip install --quiet -r requirements.txt
      - run: echo "${{ secrets.ENV_B64 }}" | base64 -d > .env
      - name: Run smatleads sync
        env:
          MAX_SEARCHES: ${{ github.event.inputs.max_searches || '333' }}
          FORCE_IN: ${{ github.event.inputs.force }}
          DRY_IN: ${{ github.event.inputs.dry }}
        run: |
          while IFS= read -r _l; do case "$_l" in '#'*|'') ;; *=*) export "${_l?}" 2>/dev/null || true ;; esac; done < .env 2>/dev/null || true
          # THE ENGINE VERSION IS A PROPERTY OF THE CODE, NOT OF THE ENVIRONMENT.
          # .env carries COMPLIANCE_ENGINE_VERSION, and the loop above exports it AFTER the job env is applied. So a
          # stale value in the secret PINS a fresh checkout: v25 code built a payload stamped v24, the database gate
          # correctly rejected the row as a stale worker, and the mint failed with "audit_pages INSERT failed - no
          # row written". The stale-worker lock was defeated by the very thing it exists to detect.
          # E-273 re-asserted MINT_CONCURRENCY and MINT_BUILD_TIMEOUT_MS from the workflow and MISSED THIS ONE. An
          # env var that can rename the engine's own version is a footgun: it can make a fresh checkout look stale,
          # and a stale checkout look fresh. Unset it. The constant in compliance.js is the single source of truth.
          unset COMPLIANCE_ENGINE_VERSION
          echo "[engine] effective ENGINE_VERSION=$(node -e "process.stdout.write(require('./src/skills/S008-personalisation-engine/scanners/compliance.js').ENGINE_VERSION)")"
          MAX="$MAX_SEARCHES"
          FORCE_FLAG=""
          DRY_FLAG=""
          if [ "$FORCE_IN" = "true" ]; then FORCE_FLAG="--force"; fi
          if [ "$DRY_IN" = "true" ]; then DRY_FLAG="--dry"; fi
          node scripts/smatleads-sync.js --max "$MAX" $FORCE_FLAG $DRY_FLAG
