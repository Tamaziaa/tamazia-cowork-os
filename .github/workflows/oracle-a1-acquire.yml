name: Oracle A1 Ampere — auto-acquire (daily retry)
# Tries once per day whether VM.Standard.A1.Flex has become available in uk-london-1.
# Oracle free tier A1 capacity is released sporadically — this catches it without the
# 25x/day noise of the original */10-minute version (deleted 2026-06-19, commit 7fa36c2,
# reason: "fired 25x/day on the dead Oracle box; closes the Z15-03 no-timeout + a Z15-02
# concurrency offender + Z15-18 waste"). This restore keeps the same launch logic but
# moves to a once-daily cron, adds continue-on-error at the job level, and caps the job
# at 15 minutes so it cannot repeat the original no-timeout defect.
#
# Restored per founder ruling D3 (DECISIONS-2026-09-04.md): "follow recommended: Hetzner
# now at best capacity, Oracle A1 retried daily as the upgrade, paid resize as fallback."
# This PR does NOT merge itself — merging to main (which arms the schedule) is the
# founder's act.
#
# REQUIRED GITHUB SECRETS (already present on this repo, unchanged since 2026-06-16):
#   OCI_CLI_USER          — OCID of the OCI user (ocid1.user.oc1..xxx)
#   OCI_CLI_FINGERPRINT   — API key fingerprint (xx:xx:xx:...)
#   OCI_CLI_TENANCY       — OCID of the tenancy (ocid1.tenancy.oc1..xxx)
#   OCI_CLI_REGION        — uk-london-1
#   OCI_CLI_KEY_CONTENT   — PEM private key content (the API signing key)
#   OCI_SUBNET_ID         — OCID of the subnet to place the instance in
#   OCI_IMAGE_ID          — Canonical Ubuntu 22.04/24.04 aarch64 image OCID for uk-london-1
#
# OPTIONAL (Telegram notify on success — silently skipped if not set):
#   TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
#
# DISABLE: comment out the cron trigger, or delete this workflow file.

on:
  schedule:
    - cron: '10 4 * * *'  # Once daily, 04:10 UTC
  workflow_dispatch:
    inputs:
      ocpus:
        description: 'Number of OCPUs (max 4 on free tier)'
        default: '4'
      memory_gb:
        description: 'Memory in GB (max 24 on free tier)'
        default: '24'

jobs:
  acquire-a1:
    runs-on: ubuntu-latest
    name: Try to acquire A1.Flex instance
    timeout-minutes: 15
    continue-on-error: true
    steps:
      - name: Install OCI CLI
        run: pip install oci-cli --quiet 2>&1 | tail -3 || true

      - name: Check if instance already exists
        id: check
        env:
          OCI_CLI_USER: ${{ secrets.OCI_CLI_USER }}
          OCI_CLI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
          OCI_CLI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
          OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}
          OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_CLI_KEY_CONTENT }}
        run: |
          mkdir -p ~/.oci
          printf '[DEFAULT]\nuser=%s\nfingerprint=%s\ntenancy=%s\nregion=%s\nkey_file=%s\n' \
            "$OCI_CLI_USER" "$OCI_CLI_FINGERPRINT" "$OCI_CLI_TENANCY" "$OCI_CLI_REGION" "~/.oci/key.pem" > ~/.oci/config
          printf '%s' "$OCI_CLI_KEY_CONTENT" > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem
          EXISTING=$(oci compute instance list \
            --compartment-id "$OCI_CLI_TENANCY" \
            --display-name "tamazia-engine-a1" \
            --lifecycle-state RUNNING \
            --query "data[0].\"public-ip\"" \
            --raw-output 2>/dev/null || echo "")
          if [ -n "$EXISTING" ] && [ "$EXISTING" != "null" ]; then
            echo "Instance already RUNNING at $EXISTING"
            echo "ALREADY_EXISTS=true" >> $GITHUB_OUTPUT
          else
            echo "ALREADY_EXISTS=false" >> $GITHUB_OUTPUT
          fi

      - name: Try each Availability Domain
        if: steps.check.outputs.ALREADY_EXISTS == 'false'
        continue-on-error: true
        env:
          OCI_CLI_USER: ${{ secrets.OCI_CLI_USER }}
          OCI_CLI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
          OCI_CLI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
          OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}
          OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_CLI_KEY_CONTENT }}
          OCI_SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          OCI_IMAGE_ID: ${{ secrets.OCI_IMAGE_ID }}
          OCPUS: ${{ github.event.inputs.ocpus || '4' }}
          MEMORY_GB: ${{ github.event.inputs.memory_gb || '24' }}
        run: |
          mkdir -p ~/.oci
          printf '[DEFAULT]\nuser=%s\nfingerprint=%s\ntenancy=%s\nregion=%s\nkey_file=%s\n' \
            "$OCI_CLI_USER" "$OCI_CLI_FINGERPRINT" "$OCI_CLI_TENANCY" "$OCI_CLI_REGION" "~/.oci/key.pem" > ~/.oci/config
          printf '%s' "$OCI_CLI_KEY_CONTENT" > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem

          set +e  # Don't abort on oci CLI non-zero exit (capacity errors return exit 1)
          SUCCESS=false
          for AD in "HzzF:UK-LONDON-1-AD-1" "HzzF:UK-LONDON-1-AD-2" "HzzF:UK-LONDON-1-AD-3"; do
            echo "Trying $AD ..."
            RESULT=$(oci compute instance launch \
              --availability-domain "$AD" \
              --compartment-id "$OCI_CLI_TENANCY" \
              --shape "VM.Standard.A1.Flex" \
              --shape-config "{\"ocpus\": $OCPUS, \"memoryInGBs\": $MEMORY_GB}" \
              --image-id "$OCI_IMAGE_ID" \
              --subnet-id "$OCI_SUBNET_ID" \
              --display-name "tamazia-engine-a1" \
              --assign-public-ip true \
              --metadata '{"ssh_authorized_keys":""}' \
              2>&1)
            EXIT_CODE=$?
            echo "Exit code: $EXIT_CODE | Response: $(echo "$RESULT" | head -c 300)"
            if echo "$RESULT" | grep -q '"lifecycle-state": "PROVISIONING"'; then
              PUBLIC_IP=$(echo "$RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('data',{}).get('public-ip','pending'))" 2>/dev/null || echo "pending")
              echo "SUCCESS on $AD — IP: $PUBLIC_IP"
              echo "INSTANCE_AD=$AD" >> $GITHUB_ENV
              echo "INSTANCE_IP=$PUBLIC_IP" >> $GITHUB_ENV
              SUCCESS=true
              break
            else
              echo "No capacity on $AD (expected 'Out of host capacity') — trying next"
            fi
          done
          set -e

          if [ "$SUCCESS" = "false" ]; then
            echo "All ADs at capacity — will retry on tomorrow's 04:10 UTC tick"
            echo "## Oracle A1 — no capacity today" >> $GITHUB_STEP_SUMMARY
            echo "All three uk-london-1 availability domains returned no capacity. Retrying tomorrow at 04:10 UTC." >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

      - name: Notify on success
        if: env.INSTANCE_IP != ''
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          MSG="Oracle A1 Ampere CREATED on $INSTANCE_AD — IP: $INSTANCE_IP (4 OCPU / 24GB). Run bootstrap script: bash ops/searxng-setup.sh"
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}&text=${MSG}"
          fi
          echo "$MSG"
          # Log to workflow summary
          echo "## Oracle A1 Created" >> $GITHUB_STEP_SUMMARY
          echo "- **AD:** $INSTANCE_AD" >> $GITHUB_STEP_SUMMARY
          echo "- **IP:** $INSTANCE_IP" >> $GITHUB_STEP_SUMMARY
          echo "- **Next:** Run \`bash ops/searxng-setup.sh\` on new VM, update SEARXNG_URL in ENV_B64" >> $GITHUB_STEP_SUMMARY
