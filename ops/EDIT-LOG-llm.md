# EDIT-LOG — LLM-RESCUE build (branch `v4-llm-rescue`, base `c017a48`)

Generation-first LLM rescue layer + human review loop for the Tamazia agency engine.
North star: the LLM LIFTS leads INTO Tier-1 by finding missing public signals — it never relaxes the gate.

Hard rules honoured throughout: deterministic gate keeps final say; LLM only PROPOSES (advisory `qa_*` columns);
consent/entity (PECR) gate never bypassed; net Tier-1 only goes UP (rescue adds, fact-check flags for human only,
never auto-demotes); LinkedIn via SERP TITLES only (SearXNG/Brave/DDG result URLs — linkedin.com is never fetched);
£0 free-models-first with a hard cap + kill switch (`LLM_QA_ENABLED`, default OFF); SEND stays OFF; no mass Neon
writes (small samples only; the backlog runs via the scheduled job after merge).

| # | Commit | File(s) | What | Syntax | Proof |
|---|--------|---------|------|--------|-------|
| 1 | `fix(mystrika): inject canonical Art-14 footer into the live push path (B-1)` | `scripts/push-to-mystrika.js` | Port the proven Mystrika `complianceFooter()` + add `withFooter()`; wrap every rendered touch body (t0-t3) so the LIVE push wire carries provenance + `{{ unsubscribe }}` + filled `{{privacy_notice_url}}`. Founder-blocked `{{reg_address}}/{{company_number}}/{{ico_number}}` stay as placeholders. | jsc PASS (ReferenceError on require at L6, no SyntaxError) | Rendered body ends with provenance + unsubscribe + privacy URL; idempotent; empty-in→empty-out. All 8 assertions true. |
| 2 | `feat(llm): generation-first rescue worker (find missing public signals → re-run gate)` | `src/lib/llm-rescue.js`, `scripts/run-llm-rescue.js` | Wave worker over non-Tier-1 leads, ordered by Part-C yield (missing_linkedin → missing_dm → missing_both → classify_sector → missing_email). Finds the missing piece (LinkedIn via SERP titles only; named DM via CH reg-officers / SERP; email via find-every-email + free verify; sector via LLM classify) using the free-first router, re-runs the CANONICAL `scoreLead()` WITH the found data, and writes advisory `qa_found/qa_suggested_tier/qa_reason/qa_confidence/qa_model/qa_status/qa_checked_at` (+ `review_status` auto_promote / unreviewed). Kill switch `LLM_QA_ENABLED` (default OFF); excludes `consent_required`; never writes icp_tier/send state; never demotes. | jsc PASS (both files) | Eval in Commit 7. |
| 3 | `feat(llm): conservative Tier-1 fact-check (flag for human review, never demote)` | `src/lib/llm-factcheck.js`, `scripts/run-llm-factcheck.js` | Conservative pass over the 614 Tier-1: deterministic doubt detectors (role-inbox email via canonical `isRoleLocal`, placeholder email, non-personlike/single-token/role-shaped DM name, no LinkedIn, listicle company name, consent-required entity) + a borderline LLM "is this a real person" adjudication (free router, gated on `LLM_QA_ENABLED`). Flags doubtful rows `qa_status='flagged'` + `review_status='unreviewed'`; clean ones `confirmed`. SQL scope-guarded to `icp_tier=1`; NEVER demotes (net Tier-1 cannot drop). | jsc PASS (both) | Deterministic flags proven in Commit 7 eval. |
| 4 | `feat(llm): apply-review job — accept/auto-promote/needs_info/rejected (consent-gated)` | `scripts/apply-review.js` | Acts on `review_status`: `auto_promote` re-runs the CANONICAL `scoreLead()` with `qa_found` and promotes to Tier-1 ONLY if the gate's own verdict is tier 1 AND not consent-required (else hands to human); `accepted` promotes to `qa_suggested_tier` by human authority but STILL blocks consent-required entities (PECR never bypassed); `needs_info` stamps `review_note` as an enrich hint + resets lifecycle to re-run qualify+rescue; `rejected` parks out of the cold path (not suppression). Promotion writes the same columns as qualify-and-queue + a governor-release attempt. Idempotent (`applied_*`). SEND OFF. | jsc PASS | `--dry` proven in Commit 7. |
| 5 | `feat(llm): NocoDB lead-review SQL (v_lead_review + editor grant) — OUTPUT ONLY` | `ops/nocodb-lead-review.sql` | `v_lead_review` read-only view (exact spec columns: `lead_ref, company, sector, tier, quality_score, qa_reason, qa_suggested_tier, qa_found, review_status, review_note` + decision context), filtered to rescued/flagged/reviewed leads, ordered by `qa_confidence`. Column-scoped `GRANT UPDATE (review_status, review_note) ON leads TO nocodb_editor` (mirrors the existing `nocodb-layers-2-4.sql` editor pattern — additive if the role exists) so only those two cells are editable; plus Excel export/import notes (resumable by `lead_ref`). NOT executed by the agent (output for founder to apply). | view body run read-only against live Neon (exit 0, resolves) | n/a — DDL handed off. |
| 6 | `feat(llm): wire gated llm-rescue/factcheck/apply-review into the cycle + backlog workflow` | `scripts/run-engine-cycle.sh`, `.github/workflows/llm-rescue-backlog.yml` | Cycle: insert the three steps after `qualify-and-queue` and before `governor-release` so promoted leads flow through enqueue→mint the same cycle. Gated on `LLM_QA_ENABLED` via a PORTABLE `case` glob (BSD/macOS sed lacks `\|`); default OFF (unset/0/false/no/off → SKIP). rescue/factcheck unguarded (advisory writes); apply-review `run_guarded` (writes icp_tier on promote). Dedicated backlog workflow: twice-daily heartbeat-wrapped waves, same kill switch (force-enable via dispatch `enable=1`), cost-capped. | `bash -n` OK; kill-switch truth table verified; `yaml.safe_load` OK | Truth table: only 1/true/yes/on RUN. |
