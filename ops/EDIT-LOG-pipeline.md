# EDIT-LOG — pipeline + observability (FIX AGENT ENG-B · branch `v4-fix-pipeline`)

> Clusters 2-4 of `Tamazia-Remix/ops/BUG-LEDGER-V4.md`: the dark reply/attribution/recycle half, the stalled
> send-prep chain (matters at send-time; SEND OFF), and observability truth. Branch off `origin/main` (2029558).
> Neon ADDITIVE only. SEND OFF (`SEND_ENABLED` master gate in send-due.js is untouched). No em dashes.
> One commit per fix `fix(pipeline/obs): <id> ...`.

## Live evidence captured at start (read-only Neon `/sql`, 2026-06-15)
- `sends`: 184 rows, **ALL `lead_id` NULL** (the warmup pool; 0 attributed).
- `inbound_emails`: 832 rows, **0 matched** (`matched_lead_id` NULL on every row) — the matcher never ran.
- `leads.audit_verified=TRUE`: 35. `leads.governor_released_at IS NOT NULL`: **0** (governor decorative).
- `leads.first_contacted_at IS NOT NULL`: **0** (no writer).
- gen-state/MCP `email_ready` = **32** vs the REAL push WHERE-clause count = **25** (divergence O5/O6).

## Path map (ledger names → real repo paths)
- `send-due.js` = `src/skills/S065-touch-scheduler/scripts/send-due.js`
- `push-to-mystrika.js` = `scripts/push-to-mystrika.js` · `mystrika-export.js` = `scripts/mystrika-export.js`
- `run-engine-cycle.sh` = `scripts/run-engine-cycle.sh` · `recycle.js` = `scripts/recycle.js`
- `check-stuck-jobs.js`/`compute-metrics.js`/`gen-state.js`/`heartbeat.js` = `scripts/*`
- `render.js` (S064) = `src/skills/S064-touch-cadence/scripts/render.js`
- `canonical-schema.json`/`.sql` = `schema/canonical-schema.{json,sql}`
- `governor.js` = `src/lib/governor.js`

## Fixes

| id | file:line | change | syntax | evidence |
|---|---|---|---|---|
| P1 [A82/X11] | `.github/workflows/match-inbound-replies.yml` | Added hourly cron (`17 * * * *`) + `engine-db-work` concurrency to the dispatch-only matcher; heartbeat-wrapped the run step (`engine_runs` job=`match-inbound-replies`). | YAML OK | 832 inbound rows, 0 matched (matcher never ran on its own). Script already self-provisions `match_method`, idempotent, no-send. |
| P2 [A21/A83/X10] | `send-due.js:164`, `push-to-mystrika.js:203` | `sends.lead_id` stamping at send was ALREADY present in BOTH paths (verified) — both INSERT `sends` with the real `lead_id` and guard non-integer ids. No code change needed in the senders; the warmup-filter half lands in P4 (funnel readers). | jsc PASS (ReferenceError: require) | 184 sends all `lead_id` NULL today = warmup pool; real sends will attribute going forward. |
| P3 [X12] | `send-due.js:171`, `push-to-mystrika.js:214` | Stamp `first_contacted_at=COALESCE(first_contacted_at,NOW())` on the FIRST contact: touch 0 in send-due.js, and at `mystrika_pushed=TRUE` in push (push enqueues touch-0). COALESCE so re-send/recycle never moves the original date. | jsc PASS | `first_contacted_at IS NOT NULL`=0 live; recycle.js parks on `first_contacted_at + NOREPLY_DAYS`, so the park step was dead. |
| P6 [X9] | `push-to-mystrika.js:65` | Added `AND l.governor_released_at IS NOT NULL` to the push WHERE clause so only governor-released leads are pushed (was decorative; push ignored the cap). Chain becomes qualify -> governor-release (P5) -> push. | jsc PASS | `governor_released_at IS NOT NULL`=0 live; P5 wires governor-release into the cycle so the gate fills. SEND OFF. |
| P4 [A33/A40/X20/X21/X45] | `server.py:134-181`, `gen-state.js:29` | (1) `Q_REPLIED`/`replied` standardised on `matched_lead_id IS NOT NULL` + exclude STOP/bounce. (2) `Q_SENT`/`sent` filter warmup (`lead_id IS NOT NULL`). (3) `Q_BOOKED`/`booked` exclude cancelled/rejected/no-show. | py_compile + jsc PASS; all 4 SQL live-run OK | sent 184->0 (all warmup); booked over empty `cal_bookings`; reply metric now consistent MCP<->gen-state. |
| O1 [A12] | `check-stuck-jobs.js:28` | Added `compute-metrics` (1440m) + `deliverability-guard` (10080m) to the stuck-detection CADENCE; also added `match-inbound-replies` (60m, from P1). | jsc PASS | both are heartbeat-wrapped (nightly-workers.yml / deliverability-guard.yml) but were absent from the liveness map. |
| O2 [A13/A42] | `server.py:303` (reader) | `engine_health`'s stuck list now excludes the DATA metric (`category='data'` + `stuck_leads`) so a lead-cadence backlog no longer masquerades as a dead engine. `stuck_*` reserved for liveness. WRITER rename (`stuck_leads`->`data_stuck_leads` in health-check.js) is OUT of my exclusive set -> DEFERRED note. | py_compile PASS | health-check.js writes `stuck_leads` (category='data'); check-stuck-jobs.js writes `stuck_<job>` (category='liveness'); both land under `stuck_%`. |
| O7 [A81] | `run-engine-cycle.sh:20` | Preflight `python3 -c "import pg8000.native"` at cycle start; on failure fire `notify-event.js stuck` (+ curl Telegram fallback) and log loudly. Non-fatal. | `bash -n` PASS; pg8000 imports locally | shim is pg8000-backed; missing pg8000 = every pg() silently no-ops = total observability blackout. |
| O3 [A30/A58] | `canonical-schema.json` (+`.sql`) | Added `notifications` + `cost_ledger` as top-level table specs (columns + pk + `create` + indexes) mirroring the LIVE Neon shapes exactly. | JSON valid; **column parity vs live = exact for both**; SQL parens balanced | both tables were live but absent from the spec, so the drift guard was blind + a fresh DB would not create them. |
| O4 [A1/A61/A64] | `canonical-schema.{json,sql}`, `gen-state.yml:30`, `check-stuck-jobs.js:31`, `gen-state.js:33/40`, `server.py:326` | (1) `engine_runs(job,started_at)` index added (json `indexes` + .sql). (2) heartbeat-wrapped `gen-state.yml` + added `gen-state` to the stuck CADENCE. (3) `checked_at` age stamped on both health surfaces (gen-state header "checked Xm ago / STALE"; MCP engine_health "Health surface last computed: ..."). | jsc + py_compile + YAML PASS; health-age SQL live-run (63m/64m) | health surfaces showed a % with no freshness; `eval-qualifier.yml` (not my file) left as DEFERRED. |
| O5/O6 [A20/X3/A51] | `server.py:137`, `gen-state.js:29` | `Q_EMAIL_READY`/`emailReady` now MIRROR the real push WHERE clause verbatim (quality_fit+qualified+audit_verified+audit_url+email+not-pushed+deliverability-clean+not-replied+status-clean+governor-released). | py_compile + jsc PASS; SQL live-run OK | old proxy 32 vs true sendable 25 (and 0 until governor releases, by design). Catch-all kept STRICT. |
| P5 [X2/X8] | `run-engine-cycle.sh:66,74`, `nightly-workers.yml:45` | Added `governor-release.js` (after qualify) + `verify-audits.js 6` (after mint) as their OWN race-guarded cycle steps; REMOVED `governor-release.js` from the single 60-min nightly `bash -c` bundle. | `bash -n` PASS; YAML OK | `governor_released_at`=0 (bundle timed out / never ran); `audit_verified`=35 starved (verify-audits was never a step). Now both run every 30 min. SEND OFF. |
| P8 [X22/X23/X24/B5] | `footer.txt` + `campaigns/_footer.txt` (reconciled byte-identical), `send-due.js:7`, `mystrika-export.js:31` | Reconciled the two footers to ONE canonical Art-14 footer (Founder/credential + Tamazia Ltd entity + `{{company_number}}`/`{{ico_number}}`/`{{reg_address}}` placeholders + "how we found you" provenance + legitimate-interest line + visible unsubscribe + powered-by/not-legal-advice disclaimer). INJECTED in BOTH paths: send-due.js `__SIGNATURE__`->`name + footer` (fills `{{privacy_notice_url}}`->`/legal/cold-outreach-privacy-notice/`, `{{unsubscribe_url}}`-> UNSUB_ENDPOINT link or reply-fallback); mystrika-export.js appends footer to each body (`{{ sender }}` sig + Mystrika `{{ unsubscribe }}`). | jsc PASS (both); footers `diff` identical; **functional test: privacy URL filled, unsubscribe filled, founder-blocked `{{...}}` preserved, no stray unfilled braces, EU line dropped** | footer was in NEITHER path (send-due put just a name; mystrika-export stripped `__SIGNATURE__`). Footer injected AFTER the lint gate so placeholders never block. SEND OFF. |
| P7 [X7/X26/X31] | `render.js` (rewritten), `send-due.js:18` | S064 now renders from the founder-reviewed `campaigns/<CODE>.json` (map via `sector_code` then slug fallback) instead of hardcoded templates. Substitutes `{{first_name/company/city/audit_url/finding/clause/calendar_link}}`; resolves T0's `[bracket]` finding; greet from `contact_name` first token (honorific-stripped) when `first_name` empty; `{{finding}}` waterfall (audit pointer -> `pp.top_finding` -> rank sentence -> campaign sector-default, never empty). Renders touches 0-3; `send-due.js` CADENCE_DAYS aligned to `[0,3,7,12]`. Touch 4 (breakup) DEFERRED (scheduler is 4-touch). Backward-compat `buildTouch0-3` exports kept (now campaign-backed). | jsc PASS (render+send-due); **functional test: all 40 touches (10 campaigns × 4) pass the gate** incl. worst-case empty-finding/no-name lead | 1,836 leads have empty first_name + contact_name; `sector_code` populated for ~6,100 leads (HO 1499, RE 1395, LS 906, HC 840...). |

> Commits (branch `v4-fix-pipeline`, in order): P1 `9cf7756` · P3+P6 `3be16a0` · P4+O5/O6 `648f2af` · P5 `aa30d46` · P7 `09e9676` · P8 `4bc6010` · O1+O2+O7 `58c31cb` · O3+O4 `2f678c6`. (P2 verified-already-present, no separate code commit.)

## Deferrals (and why)
- **P2 code in the senders**: `sends.lead_id` stamping was ALREADY present in both send paths (send-due.js + push-to-mystrika.js) before this work; no senders change was needed. The funnel-side warmup filter (the other half of P2) shipped under P4.
- **O2 writer rename**: renaming the data metric key `stuck_leads`->`data_stuck_leads` belongs in `scripts/health-check.js`, which is OUTSIDE my exclusive file set (ENG/health owns it). I fixed the READER side (server.py engine_health excludes the data metric from liveness). Hand-off: rename the `band('stuck_leads','data',...)` call in health-check.js to `data_stuck_leads` and any other readers.
- **O4 eval-qualifier.yml**: this scheduled workflow lacks a heartbeat but is not in my exclusive set (eval-qualifier.js is the audit/qualifier eval). Heartbeat-wrap it the same way as gen-state.yml when its owner touches it.
- **P7 touch 4 (breakup nudge)**: the campaigns carry 5 touches (0-4); the scheduler (send-due.js) + push read t0..t3 only. Rendering a 5th draft would orphan it, so touch 4 is deferred until the scheduler carries a 5th touch (a larger send-path change). Touches 0-3 ship the founder-reviewed copy + cadence today.
- **P9 (mint backlog / split cycle / backfill audit_slug)**: listed in the ledger's Cluster 3 but NOT in my fix list, and the mint files (mint-worker.js / enqueue) are not in my exclusive set (mint borders the audit engine). Left for the audit/mint owner.
- **Founder-blocked**: footer `{{company_number}}` / `{{ico_number}}` / `{{reg_address}}` left as placeholders per the prompt; `{{unsubscribe_url}}` falls back to the reply-"unsubscribe" instruction until `UNSUB_ENDPOINT` is set.

## Only-my-files confirmation
All 16 changed files are within the exclusive set (or the explicitly-allowed "workflow files you add heartbeats/crons to" / the required edit log). NONE of the ENG-A files were touched (enrich.js, lead-quality.js, icp.js, dm-*.js, adapters.js, resolve-name.js, qualify-and-queue.js, firmographics.js, find-every-email.js). Neon writes are ADDITIVE only (no rename/drop). SEND remains OFF (`SEND_ENABLED` master gate untouched); the send-path fixes are correct for WHEN the founder flips it and do not send now. Catch-all gate kept STRICT.
</invoke>
