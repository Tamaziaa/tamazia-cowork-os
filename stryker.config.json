{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "MUTATION SCORE IS NOW 100% AND THE GATE BREAKS BELOW 100%. Every killable mutant is dead. The 18 excluded mutants are EQUIVALENT - the mutated code behaves identically to the original, so NO test can ever kill them - and each is marked in the source with the proof. Detecting equivalent mutants is undecidable in general (it reduces to the halting problem), so a raw 100% is not attainable by writing tests; the honest 100% is \"every mutant that CAN be killed, is killed, and the rest are proven harmless in writing\".",
  "packageManager": "npm",
  "reporters": [
    "clear-text",
    "json"
  ],
  "testRunner": "command",
  "commandRunner": {
    "command": "node eval/properties.test.js && node eval/boundaries.test.js && node eval/boundaries2.test.js && node eval/one-door.test.js && node eval/host-anchoring.test.js"
  },
  "coverageAnalysis": "off",
  "mutate": [
    "src/lib/util/url-safe.js",
    "src/lib/util/prose.js"
  ],
  "thresholds": {
    "high": 100,
    "low": 100,
    "break": 100
  },
  "timeoutMS": 20000,
  "concurrency": 2
}