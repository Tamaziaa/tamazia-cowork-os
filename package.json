{
  "name": "tamazia-cowork-os",
  "version": "1.0.0",
  "private": true,
  "description": "Tamazia Cowork OS engine. Node scripts use built-ins only; deps kept minimal so npm install never fails the cycle.",
  "scripts": {
    "cycle": "bash scripts/run-engine-cycle.sh",
    "pulse": "node scripts/intel-pulse.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1120.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "acorn": "^8.17.0",
    "acorn-walk": "^8.3.5",
    "fast-check": "^4.9.0",
    "zod": "^4.4.3"
  }
}
