{
  "summary": "Find the best free/open full-site crawler (every page, every word, fast) + the word-level flagging + throughput integration for the compliance engine",
  "agentCount": 4,
  "logs": [],
  "result": {
    "plan": "I have everything I need. The ground truth materially changes the recommendation: this repo deliberately uses **Node built-ins only** (no `crawlee`, no `cheerio`, no npm deps beyond an AWS SDK), and already has a working `gatherCorpus` with retry/UA/challenge-detection, JS-reader fallback, Wayback fallback, sitemap+link discovery, and tiered prioritization. The honest plan reconciles the research with that reality rather than bolting on a heavy dependency. Here is the synthesized implementation plan.\n\n---\n\n# Implementation plan — free, full-site, every-word compliance crawler\n\n## 0. The one correction that governs everything\n\nThe crawler-repos research recommends `npm i crawlee`. **Do not.** `package.json` states the hard constraint verbatim: *\"Node scripts use built-ins only; deps kept minimal so npm install never fails the cycle.\"* The repo has no `cheerio` and no `crawlee` in `node_modules`, and `gatherCorpus` already does — with zero deps — what Crawlee's `CheerioCrawler` would do: `fetchWithRetry` (Node `fetch` + UA spoofing + challenge detection + retry/backoff in `http.js`), `_discoverLinks` (regex link extraction), `_discoverSitemap` (sitemap + child-sitemap walk), tiered prioritization (`_TIER1`/`_TIER2`), a `_renderViaReader` SPA fallback (r.jina.ai), and a Wayback fallback. Crawlee would be a regression here, not an upgrade.\n\nSo the deliverable is **not** \"adopt a new crawler library.\" It is: **extract and harden the crawl logic that already lives inside `compliance.js` into a standalone `site-crawler.js` module, then add the word-level offset index on top.** Crawlee stays as a *documented Tier-2 escape hatch only* if a future SPA wave defeats the existing reader fallback.\n\n---\n\n## (1) #1 recommended free crawler\n\n**Pick: the repo's own built-in `fetch`-based crawler (Node ≥18 global `fetch` + `AbortController`), extracted from the existing `gatherCorpus`.** Zero dependencies, already battle-tested against `pwc.co.uk`/`premierinn.com` WAFs (per the `http.js` comments). It returns exactly the `{url, fullText, html}` shape requested.\n\nIf a true dependency is ever unavoidable, the research's #1 (`crawlee` `CheerioCrawler`, Apache-2.0) is the correct fallback — but it violates the built-ins-only rule today.\n\n**Install:** none. Uses Node built-ins. (Only if escalating to Tier-2 later: `npm i crawlee playwright && npx playwright install chromium`.)\n\n**Minimal Node snippet returning `{url, fullText, html}` per page** (built-ins only, mirrors `_stripText` so text matches the matcher exactly):\n\n```js\n// minimal-crawler.js — zero deps, Node >=18\nconst RX_HREF = /href\\s*=\\s*[\"']([^\"'#?]+)/gi;\nconst UA = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36';\n\nfunction stripText(html) {\n  return String(html || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ').replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/gi, ' ').replace(/&amp;/gi, '&').replace(/&[a-z]+;/gi, ' ')\n    .replace(/\\s+/g, ' ').trim();\n}\nasync function getPage(url, ms = 8000) {\n  const ctl = new AbortController(); const t = setTimeout(() => ctl.abort(), ms);\n  try {\n    const r = await fetch(url, { redirect: 'follow', headers: { 'User-Agent': UA }, signal: ctl.signal });\n    const html = await r.text();\n    return { url: r.url || url, status: r.status, fullText: stripText(html), html };\n  } catch { return null; } finally { clearTimeout(t); }\n}\nasync function crawlSite(start, { maxPages = 25 } = {}) {\n  const origin = new URL(start).origin, seen = new Set(), out = [], queue = [start];\n  while (queue.length && out.length < maxPages) {\n    const u = queue.shift().split('#')[0]; if (seen.has(u)) continue; seen.add(u);\n    const page = await getPage(u); if (!page || page.status >= 400 || page.fullText.length < 50) continue;\n    out.push({ url: page.url, fullText: page.fullText, html: page.html });   // {url, fullText, html}\n    let m; while ((m = RX_HREF.exec(page.html)) && queue.length < 400) {\n      try { const abs = new URL(m[1], origin).toString(); if (abs.startsWith(origin) && !seen.has(abs)) queue.push(abs); } catch {}\n    }\n  }\n  return out;  // [{ url, fullText, html }, ...]\n}\nmodule.exports = { crawlSite, stripText };\n```\n\nThe production module (section 5) is this plus the existing tiered priority, sitemap discovery, reader/Wayback fallbacks, and challenge handling already proven in `gatherCorpus`.\n\n---\n\n## (2) Bounded full-site crawl design (blogs + policy + key pages always included)\n\nReconciliation: **scan every word of every page you keep; bound which pages you keep.** Compliance signal is concentrated; a 5,000-page site carries it on ~30 pages. Caps below are derived from the throughput math (3,000/day = 1 every ~29s avg; 4–6 concurrent slots → ~90s/site budget).\n\n| Cap | Value | Why |\n|---|---|---|\n| Max pages | **25** (current `gatherCorpus` default is `maxPages = 22` — keep ~25) | home + full policy set + blog sample, with margin |\n| Max depth | **2** from seed | compliance text is never 4 clicks deep |\n| Per-site wall-clock | **90s** hard abort-and-grade | one hostile site can't stall a slot |\n| Per-request timeout | **8s** (current uses 8–10s) | one hung socket can't eat the budget |\n| Max bytes/page | **2MB** (truncate, matches Googlebot) | bounds the \"every word\" step |\n| Total bytes/site | **20MB** | backstop |\n| Politeness | 1 in-flight/domain, ~150–300ms gap | avoids tripping WAFs into challenge walls |\n\n**Always-included priority set** — this is what preserves accuracy under the cap. `gatherCorpus` already builds it via `_TIER1`/`_TIER2`/`POLICY_PATHS`; the bounded design just formalizes reserved slots so blogs/policy never get crowded out:\n\n1. **Tier 0 — always:** `/` + top sitemap URLs (`_discoverSitemap`, already present).\n2. **Tier 1 — policy/legal, reserved slots, always fetched if discovered:** the existing `_TIER1` regex (`privacy|cookie|terms|legal|gdpr|data-protection|accessibility|complaint|modern-slavery|disclaimer|imprint|disclosure|regulat|compliance`) plus `POLICY_PATHS` guesses as backstop.\n3. **Tier 2 — content/blog sample:** `/blog`, `/news`, `/press`, `/insights` index + up to **5 most recent** articles (recency from sitemap `lastmod` or listing order). This is the \"blogs included, any line flagged\" promise — marketing/claims compliance lives here.\n4. **Tier 3 — fill remaining slots:** BFS from home nav, deduped by normalized URL (the current `used` Set on `u.split('#')[0].replace(/\\/$/,'').toLowerCase()`).\n\nDiscovery stays cheap: parse `sitemap.xml` once, score against tiers, fetch only the chosen ≤25 — a 5,000-URL sitemap costs *2 discovery fetches + 25 page fetches*, not 5,000. **One gap to close:** add a `lastmod`-aware blog-recency selector so Tier 2 deterministically picks the 5 newest articles (today the tiering is regex-only, not recency-ranked).\n\n---\n\n## (3) Every-word flagging with page + verbatim-line evidence (reusing the existing finding shape)\n\nThree concrete defects in the current `ruleCheck` block true word-level locate-everywhere flagging, and all three are fixable while keeping the exact finding object `scan` emits:\n\n**Defect A — first-hit short-circuit.** `prohibit` (L316–319) returns on the first matching page; `must_appear` (L326–330) returns on the first hit. A prohibited phrase in a page-14 blog post is never reported if the homepage also matched.\n\n**Defect B — repeated `_stripText`.** `_extractQuote` (L262–263) calls `_stripText` on full HTML again for every matching rule×page. With ~400 rules × 22 pages that is up to ~8,800 strips/site — the matcher bottleneck.\n\n**Defect C — no offset→line map.** Evidence is bounded around `m.index` within one page; there's no joined-corpus index to run one pass and map a global hit back to (URL + line).\n\n**Fix — four new helpers next to `_extractQuote` (L262) and `_isProse` (L249), reusing both verbatim:**\n\n- `buildCorpusIndex(corpus)` — **runs once per site, right after L354** (`const corpus = _cg.corpus || []`). For each page: `_stripText` **once**, split into sentences/lines using the *same* boundary set `_extractQuote` already uses (`[.!?\\u2022\\n]`), classify each line with `_isProse` **once**, and append to a single `joined` string separated by a record-separator char (`\\u241E`) that is in the boundary set so no quote bleeds across pages. Produces `{ pages, segments, joined, pageOffsets }` where `segments` is a sorted array of `{pageIdx, lineIdx, gStart, gEnd, text, prose}`.\n- `splitSentences(text)` — boundary set identical to `_extractQuote` so the legacy path and the index never drift.\n- `locateSegment(segments, offset)` — **binary search** mapping a joined-offset hit back to its page+line (O(log n)).\n- `scanRuleGlobal(re, index)` — one `/gi` `.exec` loop over `joined`; for each match, `locateSegment` → push `{ url, lineIdx, matched, line, prose }`. A 500-occurrence guard per rule caps pathological cases.\n\n**Rewire the three branches to consume occurrences, keeping the finding shape:**\n\n- **`prohibit` (L315–321):** collect **all** prose occurrences, not the first. Keep `evidence_url`/`evidence_snippet`/`evidence_quote` exactly as today (first prose occurrence) so existing consumers are untouched; **add** `occurrence_count` + `occurrences[]` (`{url, line_index, quote, matched}`) so a prohibited word in a footer, a testimonial, and a 2019 blog post each appear as a distinct, verbatim, located occurrence. This is the core \"any line anywhere is flagged\" change.\n- **`must_appear` (L322–331):** semantics are \"satisfied somewhere,\" so first prose hit still wins for `status:'hit'` — but source the hit's `evidence_quote` from a real `splitSentences` line instead of the raw `m[0].slice(0,200)` it uses today (L329). Miss shape (`checked_urls`, `rule_pattern_summary`) unchanged.\n- **`trigger_then_check` (L296–313):** run `scanRuleGlobal(triggerRe, index)` so `trigger_evidence` carries the verbatim triggering line + URL; then a cheap `index.joined.search(disclosureRe)` decides `hit_after_trigger` vs `miss`. Same finding shape.\n\n**Evidence stays verbatim, never templated.** `o.line` is a literal slice of stripped page text between two boundary chars; `_isProse` still rejects nav/footer boilerplate; if no prose line qualifies, `evidence_quote` is `null` exactly as `_extractQuote` returns today (L280). No fabrication.\n\n---\n\n## (4) Async + cached throughput plan (2,000–3,000/day on a tiny free VM)\n\n**Throughput math holds with ~10x headroom:** 5 crawl slots, ~10–20s avg audit (static path + cache), 90s worst-case ceiling. Steady-state ~15s avg → ~28,800/day headroom; even all-timeout → ~4,800/day, still above 3,000.\n\n- **Static-first, headless-rare.** Cheerio-equivalent (`fetch`+`stripText`) is the default at ~5–15MB/worker. The existing `_renderViaReader` (r.jina.ai) already serves as the **free, zero-infra SPA fallback** — no local Chromium needed at all, which is ideal for a tiny VM (Playwright's 150–300MB/browser is the thing to avoid). Keep reader-fallback gated to <5–10% of sites (empty-shell heuristic already in `gatherCorpus` L175). Wayback fallback (L189) guarantees every site grades.\n- **Cost model flips** from `O(rules × pages × strip)` to `O(pages × strip) + O(rules × |joined|)`: 22 strips/site instead of up to 8,800. Throughput levers: (a) **compile each rule's `RegExp` once** per worker (cache `rule._re`/`rule._triggerRe` — today `new RegExp` runs per site at L293/L300); (b) **`includes` pre-filter** — extract the longest `[a-z]{4,}` literal from each pattern and `index.joined.includes(literal)` before running the regex; most of ~400 rules short-circuit per site (biggest single win); (c) cap `joined` at the same **600KB** `scan` already uses for `corpusText` (L355).\n- **Per-domain corpus cache (already present, extend it).** `getCached`/`writeCache` in `http.js` keyed on `domain|sector|country`, 24h TTL (L348, L492). Extend with: store the discovered tier-map separately, send conditional `If-None-Match`/`If-Modified-Since` for 304 reuse, longer TTL (24h) for the sitemap/URL map, and a **negative cache** (~1h) for DNS-fail/403/refused so dead domains aren't retried each audit. LRU disk cap (2–5GB) so the small VM disk doesn't fill.\n- **Concurrency:** 4–6 concurrent audits on 1GB (8–10 on 2GB) via a bounded worker pool / `p-queue`-style semaphore — *not* unbounded `Promise.all`. Within a site, 4–6 parallel fetches but ≤1–2 in-flight per domain. The current `gatherCorpus` `Promise.all(fetchList.map(...))` (L163) should gain a small concurrency cap to respect per-domain politeness.\n- **Two-queue async pipeline so crawl never blocks mint.** Crawl/scan workers (concurrency 4–6, each wrapped in a 90s `AbortController`) write finished `{auditId, findings, grade}` to a results store; a **separate** mint queue/worker consumes only finished results. A site hanging 90s ties up one crawl slot, never the mint path. Idempotent with bounded retry + negative cache; a permanent failure still produces a gradeable \"could not fetch — graded on available signal\" result (the existing `blocked`/`reason` path at L358–365 already does this), so mint is never left waiting.\n\n---\n\n## (5) Exact new module path + how `gatherCorpus` uses it\n\n**New module:** `/Users/amanigga/Desktop/TAMAZIA-REBUILD/cowork-os-compliance/src/lib/compliance/site-crawler.js`\n(sits alongside the existing `connect.js`, `jurisdiction-router.js`, `tracker-detect.js`, `resolver.js` in `src/lib/compliance/`.)\n\n**Companion module (word-index, keep it next to the matcher that consumes it):**\n`/Users/amanigga/Desktop/TAMAZIA-REBUILD/cowork-os-compliance/src/skills/S008-personalisation-engine/scanners/corpus-index.js` — exports `buildCorpusIndex`, `splitSentences`, `locateSegment`, `scanRuleGlobal`. (It must reuse `_stripText`/`_isProse`; export those from `compliance.js` or move them into a tiny shared `text-utils.js` both `require`. Sharing the functions, not copying, is what prevents drift between the index path and the legacy `_extractQuote` path.)\n\n**What `site-crawler.js` exports** (it is the extracted, hardened `gatherCorpus` internals — built-ins only, importing `fetchWithRetry` from the existing `../../skills/S008-personalisation-engine/lib/http.js`):\n\n```js\nmodule.exports = { crawlSite };\n// crawlSite({ domain, maxPages = 25, depth = 2, budgetMs = 90000, signal })\n//   -> { corpus: [{ url, fullText, html, status, fetch_ms, bytes, rendered?, archived?, archive_date? }],\n//        blocked, reason, challenge, home_status, pages_tried, via_archive, archive_date }\n```\n\nIt contains the logic currently inline in `gatherCorpus` (L138–211): `_discoverLinks`, `_discoverSitemap`, `_TIER1`/`_TIER2`/`POLICY_PATHS` prioritization, the `_renderViaReader` SPA fallback, the `_archiveSnapshot` Wayback fallback, full-body dedup, and the honest `reason` classifier — **plus** the new bounds: 90s `AbortController` budget, depth cap, 2MB/page truncation, per-domain concurrency limit, and the recency-ranked blog selector. It adds `fullText` (pre-stripped via shared `_stripText`) to each corpus entry so the index never re-strips.\n\n**How `compliance.js` uses it — minimal, surgical:**\n\n1. **Replace the inline `gatherCorpus` body** with a thin wrapper (or delete the local function and import): at the top, `const { crawlSite } = require('../../../lib/compliance/site-crawler.js');`. Then `gatherCorpus({ domain })` (called at **L353**) delegates to `crawlSite({ domain, maxPages: 25 })`. The returned shape is byte-identical to today's, so L354–365 (the credibility/blocked guards), the privacy-anchor logic, jurisdiction detection, and caching all keep working unchanged.\n2. **Build the word index right after L354:** `const index = buildCorpusIndex(corpus);` using the new `corpus-index.js`.\n3. **Thread the index into the matcher:** change the loop at **L446** from `ruleCheck(r, corpus, normSector)` to `ruleCheck(r, corpus, index, normSector)`, and rewrite the three branches (L298–331) to call `scanRuleGlobal`/`locateSegment` per section (3). The `out` object keeps every existing field and only *gains* `occurrence_count` + `occurrences[]` on prohibit/multi-hit findings.\n4. **Everything downstream is untouched:** `findings`, `sevRank` sort (L478), `writeCache` (L492), jurisdiction routing, the privacy-suppression guard (L450), and the cookie-diff finding (L470) all consume the same finding shape. Richer consumers can iterate `occurrences[]` to render \"flagged on N pages.\"\n\n**Net effect:** every word of every kept page (blog, FAQ, testimonial, footer) is scanned by all ~400 rules over the `joined` corpus; any single non-compliant line anywhere is flagged with exact `url` + `line_index` + verbatim `quote` via the `segments` offset map (all such lines for `prohibit`, not just the first); evidence is always a real prose line from the existing `_stripText`→split→`_isProse` path, never templated; and the cost flip (one strip/page + `includes` pre-filter + binary-search locate) keeps the matcher at single-digit-ms CPU/site, leaving fetch latency — not matching — as the only thing between you and 2,000–3,000 audits/day on a free VM. **Free-only throughout: zero new npm dependencies; the sole \"render\" fallback is the public r.jina.ai reader plus public Wayback, both already wired.**",
    "repos": "I now have comprehensive, verified data on every requested tool. I have what I need to deliver the final ranked analysis.\n\n# Best free/open-source full-site crawlers for a Node.js compliance-audit engine (2,000–3,000 sites/day, \"every page + every word\")\n\n## TL;DR\nFor your stack (Node engine, small Linux VM, free-only, fast), the **#1 pick is Crawlee's `CheerioCrawler`** (apify/crawlee). It's native Node/TS, Apache-2.0, ~23.7k stars, actively maintained (v3.17.0, June 2026), and gives you static-HTTP speed (sub-second/page), built-in link-following (`enqueueLinks`), a `Sitemap` utility, robots.txt politeness (`respectRobotsTxtFile`), and concurrency/retry/queue management out of the box — all in-process in your Node engine. Add **`PlaywrightCrawler`** (same library, same API) as a *tier-2 fallback* only for the small fraction of JS-heavy/SPA sites where the raw HTML is empty. This tiered \"Cheerio-first, Playwright-on-demand\" design is the reliable way to hit your throughput.\n\nStrong runner-up if you ever want a single fast binary that auto-decides HTTP-vs-Chrome per page: **spider-rs** (Rust core with a real, current Node binding `@spider-rs/spider-rs`).\n\n---\n\n## Ranked comparison\n\n| # | Tool | GitHub URL | Lang | License | Stars | Speed | JS render | Sitemap + link-follow | robots.txt | Node usability | Maintained? |\n|---|------|-----------|------|---------|-------|-------|-----------|----------------------|-----------|----------------|-------------|\n| **1** | **Crawlee — CheerioCrawler** | github.com/apify/crawlee | TS/Node | Apache-2.0 | ~23.7k | Sub-second/page (raw HTTP) | No | Yes (`enqueueLinks`, `Sitemap` util, RequestQueue) | Yes (`respectRobotsTxtFile`) | Native — `npm i crawlee` | Yes, v3.17.0 Jun 2026 |\n| **2** | **Crawlee — PlaywrightCrawler** | github.com/apify/crawlee | TS/Node | Apache-2.0 | ~23.7k | Seconds/page (headless) | Yes (full) | Yes (same API) | Yes | Native, same library | Yes |\n| **3** | **spider-rs** (+ `@spider-rs/spider-rs`) | github.com/spider-rs/spider | Rust (+Node binding) | MIT | ~2.5k | Fastest (100k pages in 1–10 min); `crawl_smart` does HTTP→Chrome only when needed | Yes (Chrome feature) | Link-follow yes; sitemap not first-class | Yes (`with_respect_robots_txt`) | Real Node binding, v0.0.163 Jan 2026 | Yes (core v2.48.x Mar 2026) |\n| **4** | **Katana** | github.com/projectdiscovery/katana | Go | MIT | ~17k | Very fast (standard mode) | Yes (headless hybrid) | Yes (`-known-files=sitemapxml,robotstxt`) | Yes | Shell out to binary; JSONL out | Yes, v1.6.1 May 2026 |\n| **5** | **Colly** | github.com/gocolly/colly | Go | Apache-2.0 | ~25k | Very fast | No | Link-follow yes; sitemap manual | Yes | Separate Go service | Yes (Feb 2026) |\n| 6 | Scrapy | github.com/scrapy/scrapy | Python | BSD-3 | ~62k | Fast (async) | No (needs splash/playwright addon) | Yes (SitemapSpider, link extractors) | Yes | Separate Python service | Yes (2.14.x, 2026) |\n| 7 | node-crawler (Crawler v2) | github.com/bda-research/node-crawler | Node | MIT | ~6.5k | Fast (HTTP) | No | Link-follow manual; no sitemap | Partial | Native | Low activity |\n| 8 | gospider | github.com/jaeles-project/gospider | Go | MIT | ~3k | Very fast | No | Sitemap+robots parsing yes | Yes | Shell out | Stale |\n| 9 | website-scraper | github.com/website-scraper/node-website-scraper | Node | MIT | — | Medium | No | Recursive link-follow; mirror-to-disk | Partial | Native | Low |\n| 10 | hakrawler | github.com/hakluke/hakrawler | Go | MIT | ~5k | Fast | No | Discovery-only | Partial | Shell out | **Stale** (no release ~2 yrs) |\n| 11 | Photon | github.com/s0md3v/Photon | Python | GPL-3.0 | ~12.9k | Fast | No | URL/asset discovery | Partial | Separate; GPL viral | Minimal |\n| 12 | x-ray | github.com/matthewmueller/x-ray | Node | MIT | — | Medium | No | Selector-based pagination | No | Native | **Effectively unmaintained** |\n| — | got-scraping | github.com/apify/got-scraping | Node | Apache-2.0 | — | Fast (HTTP only) | No | None (HTTP client, not a crawler) | No | Native | **DEPRECATED** |\n\n---\n\n## Skeptical maintenance notes (verified)\n- **got-scraping is officially DEPRECATED** — \"will no longer receive updates or support.\" Don't build on it directly. Note: Crawlee historically used it internally as the CheerioCrawler HTTP client, but Crawlee is actively migrating to a pluggable `HttpClient` abstraction (impit-client / fetch-based) — issues #3275/#3314. This is a Crawlee-internal concern, handled by the maintainers; it doesn't block you.\n- **x-ray** — npm unchanged for years; \"simple pages only,\" treat as a maintenance risk. Avoid.\n- **hakrawler** — no Go-module release in ~2 years; Snyk flags it as effectively discontinued. Recon-only anyway (discovers URLs, doesn't extract full text).\n- **Photon** — GPL-3.0 (viral license — a real concern for a commercial audit product) and OSINT-oriented, not full-text extraction.\n- **spider-rs npm** — the npm landing page can read \"a year ago,\" but the binding repo (`spider-rs/spider-nodejs`) is current: **v0.0.163, Jan 2026**, with the Rust core at v2.48.x (Mar 2026). It is genuinely maintained; don't be misled by a stale npm cache string.\n- **node-crawler** — real and has a TS \"Crawler v2,\" but lower activity and you'd hand-roll sitemap + link-follow logic.\n\n## Static-HTTP vs headless (the core tradeoff)\n- **Static HTTP (Cheerio/Colly/Katana-standard/spider HTTP):** fetch + parse in ~100–800 ms/page, ~30–50 MB RAM/worker. Gets \"every word\" for ~85–95% of normal/marketing/law-firm/blog sites (server-rendered HTML). This is what makes 2–3k sites/day on a small VM realistic.\n- **Headless (Playwright/Katana-headless/spider-chrome):** 1–5+ s/page, 300–700 MB/browser, far fewer parallel workers. Needed only for true SPAs (React/Angular/Vue) where the initial HTML is an empty shell.\n\n**Reliable answer to \"every page + every word in ms–seconds\":** only the static-HTTP tier delivers it at scale. Headless is correctness insurance for the minority of JS-only sites — never your default.\n\n## Recommended tiered approach for your engine\n1. **Tier 1 (default):** `CheerioCrawler` — crawl homepage → `enqueueLinks` (same-domain) + load `sitemap.xml` via the `Sitemap` util → extract `$('body').text()` (or `$.root().text()`). Cap depth/page-count per site.\n2. **JS-detection gate:** if Tier-1 text length is below a threshold (e.g. <200 visible words) or the body is an obvious app shell, re-queue that URL into **Tier 2**.\n3. **Tier 2 (fallback, capped):** `PlaywrightCrawler` (same Crawlee API, shared queue) renders only the flagged pages. Keep a low max-concurrency for browsers.\n4. Always set `respectRobotsTxtFile: true`, a real User-Agent, `maxRequestsPerCrawl`, and `maxConcurrency` tuned to your VM cores.\n\nThis keeps ~90%+ of pages on the fast path and only pays the headless cost where it's actually required — the design that hits 2–3k sites/day on a small box.\n\n---\n\n## #1 pick: Crawlee `CheerioCrawler` — install + minimal `{url, fullText, html}` snippet\n\nInstall:\n```bash\nnpm i crawlee\n# Tier-2 fallback (optional, only if you need JS rendering):\nnpm i crawlee playwright && npx playwright install chromium\n```\n\nMinimal crawler that yields `{url, fullText, html}` for every page (homepage + internal links + sitemap URLs):\n```js\nimport { CheerioCrawler, Sitemap } from 'crawlee';\n\nasync function auditSite(startUrl) {\n  const results = [];\n\n  const crawler = new CheerioCrawler({\n    respectRobotsTxtFile: true,        // politeness\n    maxConcurrency: 10,\n    maxRequestsPerCrawl: 300,          // safety cap per site\n    requestHandler: async ({ request, $, enqueueLinks }) => {\n      const html = $.html();\n      // \"every visible word\": strip script/style, collapse whitespace\n      $('script, style, noscript, template').remove();\n      const fullText = $('body').text().replace(/\\s+/g, ' ').trim();\n\n      results.push({ url: request.loadedUrl, fullText, html });\n\n      // follow same-domain internal links (homepage, blog, etc.)\n      await enqueueLinks({ strategy: 'same-domain' });\n    },\n  });\n\n  // seed: homepage + every URL listed in the sitemap\n  const { urls } = await Sitemap.load(new URL('/sitemap.xml', startUrl).href)\n    .catch(() => ({ urls: [] }));\n  await crawler.run([startUrl, ...urls]);\n\n  return results; // [{ url, fullText, html }, ...] for every page\n}\n\n// usage\nconst pages = await auditSite('https://example.com');\nconsole.log(pages.length, 'pages,', pages.reduce((n, p) => n + p.fullText.length, 0), 'chars');\n```\n\nTo add the headless fallback, instantiate a `PlaywrightCrawler` with an identical `requestHandler` (use `page.content()` for `html` and `await page.evaluate(() => document.body.innerText)` for `fullText`) and feed it only the URLs your Tier-1 detector flagged as JS-empty.\n\nSources:\n- [apify/crawlee](https://github.com/apify/crawlee) · [CheerioCrawler docs](https://crawlee.dev/js/docs/guides/cheerio_crawler.mdx) · [got-scraping deprecation](https://github.com/apify/got-scraping) · [Crawlee httpclient migration #3275](https://github.com/apify/crawlee/issues/3275)\n- [projectdiscovery/katana](https://github.com/projectdiscovery/katana)\n- [gocolly/colly](https://github.com/gocolly/colly)\n- [spider-rs/spider](https://github.com/spider-rs/spider) · [spider-rs/spider-nodejs](https://github.com/spider-rs/spider-nodejs) · [@spider-rs/spider-rs npm](https://www.npmjs.com/package/@spider-rs/spider-rs)\n- [scrapy/scrapy](https://github.com/scrapy/scrapy) · [s0md3v/Photon](https://github.com/s0md3v/Photon)\n- [bda-research/node-crawler](https://github.com/bda-research/node-crawler) · [matthewmueller/x-ray](https://github.com/matthewmueller/x-ray) · [website-scraper/node-website-scraper](https://github.com/website-scraper/node-website-scraper)\n- [jaeles-project/gospider](https://github.com/jaeles-project/gospider) · [hakluke/hakrawler](https://github.com/hakluke/hakrawler) · [hakrawler Snyk advisor](https://snyk.io/advisor/golang/github.com/hakluke/hakrawler)",
    "flagging": "I have everything I need from the matcher. Let me design the word-level compliance flagging system that reuses the existing `_extractQuote`/finding shape.\n\n---\n\n# Word-Level Compliance Flagging — Design\n\n## Where today's engine stops short\n\nThe current `ruleCheck` (L284) already runs every rule across the full multi-page `corpus` and `_extractQuote` (L262) already pulls a verbatim sentence. But three things prevent true word-level, locate-everywhere flagging:\n\n1. **First-hit short-circuit.** For `prohibit` rules (L316) and `must_appear` hits (L326), the loop `break`s/`return`s on the *first* page that matches. A prohibited phrase appearing in a blog post on page 14 is never reported if it also appeared on the homepage — and for `must_appear` the loop stops at the first satisfying page so it never enumerates *all* offending pages.\n2. **Per-page re-scan cost.** `_extractQuote` calls `_stripText` on the full HTML *again* for every matching rule×page. With ~400 rules × ~22 pages that is up to 8,800 strips of multi-hundred-KB HTML per site — the bottleneck at 2–3k sites/day.\n3. **No offset map back to line.** Evidence is the sentence around `m.index` within a single page's stripped text. There's no joined-corpus index, so you can't run one regex pass over everything and map a global hit back to (page URL + line).\n\nThe design below fixes all three while keeping `_extractQuote`'s sentence-bounding logic, `_isProse` gating, and the exact `finding` object shape that `scan` emits.\n\n---\n\n## (a) Pre-tokenise once: the corpus index\n\nBuild the index **once per site**, immediately after `gatherCorpus`, before the rule loop. This replaces repeated `_stripText` calls with a single strip per page plus a precomputed line table.\n\n```\nfunction buildCorpusIndex(corpus) {\n  const pages = [];          // per-page: stripped text + line table\n  const segments = [];       // flat list of { pageIdx, lineIdx, start, end, text }\n  let joined = '';           // the whole site as ONE searchable string\n  const pageOffsets = [];    // joined-string offset where each page begins\n\n  for (let p = 0; p < corpus.length; p++) {\n    const text = _stripText(corpus[p].body);     // ← reuse existing _stripText, ONCE\n    // Sentence/line split: keep the SAME boundary chars _extractQuote already uses.\n    const lines = splitSentences(text);          // split on [.!?\\u2022\\n] keeping spans\n\n    pageOffsets.push(joined.length);\n    pages.push({ url: corpus[p].url, text, lines });\n\n    let cursor = 0;\n    for (let l = 0; l < lines.length; l++) {\n      const seg = lines[l];                       // { text, startInPage, endInPage }\n      segments.push({\n        pageIdx: p, lineIdx: l,\n        gStart: joined.length + seg.startInPage,   // offset in joined corpus\n        gEnd:   joined.length + seg.endInPage,\n        text: seg.text,\n        prose: _isProse(seg.text)                  // ← reuse existing _isProse, ONCE\n      });\n    }\n    joined += text + '\\n\\u241E\\n';                // \\u241E = record separator: a real\n                                                  // boundary char so no regex match can\n                                                  // straddle two pages.\n  }\n  return { pages, segments, joined, pageOffsets };\n}\n```\n\nKey properties:\n- **One strip per page, one prose-classification per line** — amortised across all 400 rules instead of per rule×page. `_stripText` and `_isProse` are reused verbatim, not reimplemented.\n- **`joined`** lets a rule run as a single `regExp` pass over the entire site (`/g` flag) instead of 22 separate `.match` calls.\n- **The page-separator char (`\\u241E`)** is in the boundary set, so `_extractQuote`'s sentence-bounding (which stops at `\\n`) naturally refuses to bleed one page's text into another's quote.\n- **`segments`** is a sorted array of global offsets. A hit at joined-offset `i` maps back to its page+line by binary-searching `segments` for the segment whose `[gStart, gEnd)` contains `i`. That is the \"offset → page+line\" map the task asks for.\n\n`splitSentences` must use the **same** boundary set as `_extractQuote` (`[.!?\\u2022\\n]`) so a flagged line is identical whether located via the global index or via the legacy per-page path — no drift between the two.\n\n---\n\n## (b) Run each rule over the joined corpus, capture EVERY hit\n\nReplace the first-hit loops with a global scan that walks all matches. New helper, sitting alongside `ruleCheck`, reusing `_extractQuote` for the quote text:\n\n```\nfunction scanRuleGlobal(re, index) {\n  // re built once per rule with the 'g' + 'i' flags\n  const rx = new RegExp(re.source, 'gi');\n  const occ = [];\n  let m, guard = 0;\n  while ((m = rx.exec(index.joined)) && guard++ < 500) {\n    if (m.index === rx.lastIndex) rx.lastIndex++;        // zero-width guard\n    const seg = locateSegment(index.segments, m.index);  // binary search → page+line\n    if (!seg) continue;\n    occ.push({\n      url: index.pages[seg.pageIdx].url,\n      lineIdx: seg.lineIdx,\n      matched: m[0].slice(0, 80),\n      line: seg.text,                 // the VERBATIM line (already split, already stripped)\n      prose: seg.prose\n    });\n  }\n  return occ;   // every occurrence across every page\n}\n```\n\nThen rewrite the three rule branches in `ruleCheck` to consume occurrences instead of breaking early. Crucially, **the evidence quote still comes from `_extractQuote`** — but now called against the single matching page's body using the segment we located, so the sentence-bounding + nav-stripping logic is preserved:\n\n- **`prohibit`** (was L315–321): instead of returning on the first page, collect **all** occurrences. The finding's primary `evidence_url`/`evidence_quote` stays exactly as today (first prose occurrence), but add an `occurrences[]` array so every offending line everywhere is carried:\n  ```\n  const occ = scanRuleGlobal(re, index).filter(o => o.prose);\n  if (occ.length) {\n    const primary = occ[0];\n    return { ...baseFinding('miss', rule),\n      evidence_url: primary.url,\n      evidence_snippet: primary.matched,\n      evidence_quote: primary.line,                 // verbatim, real line\n      occurrence_count: occ.length,\n      occurrences: occ.map(o => ({ url: o.url, line_index: o.lineIdx,\n                                   quote: o.line, matched: o.matched })) };\n  }\n  ```\n  This is the most important change: a prohibited word in a footer, a testimonial, and a 2019 blog post now each appear as a distinct `occurrence` with its own URL and verbatim line — even if the homepage also tripped the rule.\n\n- **`must_appear`** (was L322–331): semantics are \"satisfied somewhere,\" so the first prose hit still wins for `status: 'hit'`. But run the global scan over the `url_check`-filtered subset's offset range so the hit's `evidence_quote` is a real line (today it's `m[0].slice(0,200)` — the raw match, not a clean sentence). On **miss**, behaviour is unchanged (no quote — there's nothing to quote), keeping the existing `checked_urls`/`rule_pattern_summary` miss shape.\n\n- **`trigger_then_check`** (was L298–313): run `scanRuleGlobal(triggerRe, index)` for the trigger so `trigger_evidence` carries the verbatim triggering line and its URL; then a cheap `index.joined.search(disclosureRe)` to decide `hit_after_trigger` vs `miss`. Same finding shape as today, just sourced from the index.\n\n**Evidence stays verbatim, never a template.** `o.line` is a literal slice of the page's stripped text (the exact sentence between two boundary chars), and `_extractQuote` is still the function that trims leading nav runs and rejects boilerplate via `_isProse`. No finding ever fabricates or paraphrases a quote — if no prose line qualifies, `evidence_quote` is `null`, exactly as `_extractQuote` returns today (L280).\n\n---\n\n## (c) Speed at 2–3k sites/day\n\nThe cost model changes from **O(rules × pages × stripcost)** to **O(pages × stripcost) + O(rules × joinedlen)**:\n\n| Step | Today | Proposed |\n|---|---|---|\n| HTML strip | up to 400×22 per site | **22 per site** (once in `buildCorpusIndex`) |\n| Prose classify | per matched rule×page | **once per line** in the index |\n| Regex execution | 400 rules × 22 `.match` | 400 rules × 1 `.exec` loop over `joined` |\n| Locate page+line | n/a (single page) | **binary search** over `segments` (log n) |\n\nConcrete throughput levers:\n1. **Compile each rule's `RegExp` once** (cache on the rule object: `rule._re`, `rule._triggerRe`). Today `new RegExp` runs inside `ruleCheck` for every site; with a per-process rule cache it's compiled once per worker.\n2. **Cheap pre-filter before the full scan.** For `must_appear`/`prohibit` rules whose pattern has a literal anchor token (extract the longest `[a-z]{4,}` literal from the source), do `index.joined.includes(literal)` first; only run the regex if the literal is present. Most of the ~400 rules will short-circuit on the `includes` for a given site. This is the single biggest win — a plain `String.includes` over a ~600KB joined corpus is far cheaper than a regex exec, and skips it entirely for absent patterns.\n3. **Cap `joined` length** the way `scan` already caps `corpusText` at 600KB (L355). Use the same cap so memory per worker is bounded and the index is built from the same text the rest of `scan` sees.\n4. **`segments` binary search** (`locateSegment`) is O(log n) per hit; with the 500-occurrence guard per rule, worst-case locate cost is trivial.\n5. **No new I/O.** Everything operates on the corpus already fetched by `gatherCorpus`. The crawl (network) remains the only slow part; the matcher becomes CPU-bound and cache-friendly.\n\nEstimated per-site matcher cost: 22 strips + ~400 `includes` probes + a handful of full regex scans + a few dozen binary searches — comfortably single-digit milliseconds of CPU, leaving the daily budget dominated by fetch latency, not matching.\n\n---\n\n## What gets wired into `scan`\n\nMinimal, surgical changes — the `findings` array, severity ranks, caching, jurisdiction logic, and credibility guards all stay:\n\n1. After L354 (`const corpus = _cg.corpus || []`), add `const index = buildCorpusIndex(corpus);`.\n2. In the rule loop (L445–455), pass `index` into `ruleCheck(r, corpus, index, normSector)` and let the three branches use `scanRuleGlobal`. The `out` object keeps every existing field; it only *gains* `occurrence_count` and `occurrences[]` on prohibit/multi-hit findings.\n3. Consumers that read `evidence_url` + `evidence_quote` keep working unchanged; richer consumers can iterate `occurrences[]` to render \"flagged on N pages\" with each verbatim line + URL.\n\n---\n\n## Net effect\n\n- **Every word/line/sentence on every crawled page** (blog, FAQ, testimonial, footer) is scanned by all ~400 rules, because the rule runs over the `joined` corpus that concatenates every page's stripped text.\n- **A single non-compliant line anywhere is flagged** with its exact `url` + `line_index` + verbatim `quote`, via the `segments` offset map — and for prohibit rules, *all* such lines, not just the first.\n- **Evidence is always a real line**, produced by the same `_stripText` → sentence-split → `_isProse` path `_extractQuote` already uses, never a template.\n- **Fast enough for 2–3k/day** because strip + prose-classify happen once per page, a literal pre-filter skips most regex work, and hit→location is a binary search.\n\nRelevant file: `/Users/amanigga/Desktop/TAMAZIA-REBUILD/cowork-os-compliance/src/skills/S008-personalisation-engine/scanners/compliance.js` — new helpers `buildCorpusIndex`, `splitSentences`, `locateSegment`, `scanRuleGlobal` live next to `_extractQuote` (L262) and the rewritten branches inside `ruleCheck` (L284); wiring point in `scan` is right after L354.",
    "throughput": "The data confirms my priors. Most SMB sites are 10-100 pages (under 50 typical), Cheerio uses ~few MB/worker vs Puppeteer's 150-300MB, and Google itself caps at 2MB HTML/page. That's enough to write a concrete, defensible budget.\n\n# Throughput-Safe Crawl Design for Compliance Audits\n\n## The core reconciliation: \"every page, every word\" is a marketing claim, not an engineering spec\n\nThe honest resolution: **you scan every word of every page you crawl, but you bound which pages you crawl.** Compliance signals (policy pages, ToS, privacy, disclaimers, cookie banners, accessibility statements, contact/refund/shipping terms) live on a small, predictable set of pages. A 5,000-page e-commerce site has those signals on ~30 pages; the other 4,970 are product SKUs and paginated listings that repeat the same footer/template. Crawling all 5,000 buys near-zero marginal compliance signal for ~150x the cost. So the bound isn't a compromise on accuracy — it's targeting the pages that actually carry compliance text, and treating the long tail as sampled rather than exhaustive.\n\n## The math that forces the bound\n\nTarget: **3,000 audits/day = ~125/hour = ~1 every 29 seconds** of wall-clock throughput (averaged). On a 1-2GB VM you realistically run **4-6 concurrent audit slots** (see memory math below), so each audit has a **~2-3 minute wall-clock budget** to stay on pace. That is the hard ceiling every cap below is derived from.\n\nMemory ceiling on a 1GB VM (leave ~400MB for OS + Node baseline + queue/runtime → ~600MB working):\n- Cheerio/got worker: ~5-15MB resident each → 6 concurrent static workers ≈ 60-90MB. Trivially fits.\n- One headless Chromium: 150-300MB. **You can afford at most ONE shared headless instance, and only sometimes.** This is why headless must be rare fallback, not default.\n\n## (a) The caps — page / depth / time budget per site\n\n| Cap | Value | Rationale |\n|---|---|---|\n| **Max pages fetched** | **25** (hard ceiling 40) | Covers home + policy set + blog sample with margin; keeps a 5,000-page site indistinguishable in cost from a 20-page site |\n| **Max crawl depth** | **2** from seed set | Home (0) → linked policy/nav pages (1) → one hop (2). Compliance text is never 4 clicks deep |\n| **Per-site wall-clock budget** | **90 seconds hard timeout** | Abort-and-grade-with-what-you-have. A single slow/hostile site can never exceed this and stall the slot |\n| **Per-request timeout** | **8 seconds** connect+read | One hung TCP socket can't eat the site budget |\n| **Max bytes per page** | **2MB HTML** (match Googlebot), truncate beyond | Bounds the \"scan every word\" step; 2MB ≈ 400-page novel, more than any real page |\n| **Total bytes per site** | **20MB** | Backstop against 25 huge pages |\n| **Robots/rate** | 1 in-flight req per domain by default, ~150-300ms politeness gap | Avoids tripping WAFs that would force headless retries |\n\n**The \"always-included\" priority page set** (this is what preserves audit quality under the cap). Build the fetch list by priority, fill up to 25, never let low-priority pages crowd out these:\n\n1. **Tier 0 — always fetch:** `/` (home), and from `robots.txt` → `sitemap.xml` take the **top-priority + most-recently-modified** URLs.\n2. **Tier 1 — policy/compliance pages (always fetch if discovered):** match link `href`/anchor text against a regex set — `privacy`, `terms`, `tos`, `legal`, `cookie`, `gdpr`, `ccpa`, `accessibility`, `refund`, `returns`, `shipping`, `disclaimer`, `imprint`, `dmca`, `do-not-sell`, `contact`, `about`. These are the compliance core — they get reserved slots and are fetched even if it means skipping blog posts.\n3. **Tier 2 — content sample:** `/blog`, `/news`, `/press` index + up to **5 most recent** article links (recency from sitemap `lastmod` or listing order). Catches claims/marketing-language compliance.\n4. **Tier 3 — fill remaining slots:** breadth-first from home nav, dedup by URL-normalization (strip query/fragment/trailing slash, collapse `?page=`/pagination, skip asset extensions).\n\nDiscovery is cheap: parse `robots.txt` + `sitemap.xml` first (one fetch each) to get the URL universe *without crawling*, score against the tiers, then fetch only the chosen ≤25. This means a 5,000-page sitemap costs **2 discovery fetches + 25 page fetches**, not 5,000.\n\n## (b) Static-HTTP-first, headless as rare fallback\n\n**Default path (≥95% of pages):** `got` (HTTP/2, gzip/br, follow redirects, the 8s timeout) → `cheerio.load()` → extract text. ~5-15MB, ~50-300ms/page. A whole 25-page site finishes in 2-8 seconds of fetch time.\n\n**Headless fallback — gated, never automatic per-page.** Only escalate a *whole site* (not individual pages) to headless when a cheap heuristic says the static HTML is empty-shell SPA:\n- Trigger: static HTML body text < ~400 chars AND (`<div id=\"root\">`/`__next`/`ng-app`/heavy inline `<script>` with no rendered text) AND policy-link regex found zero Tier-1 matches.\n- Mechanism: **one shared, long-lived headless Chromium** for the whole VM (launched once, reused via a pool of 1-2 pages), behind a **semaphore of 1**. Audits needing headless queue for it. Cap headless to **≤8 pages/site** and a tighter **30s sub-budget**.\n- Guardrail: headless escalations must stay **<5-10% of audits** or the day's throughput collapses. Track the rate; if it climbs, the SPA detector is too loose. Disable images/CSS/fonts/media in the headless context (`request interception`) to cut its memory and time.\n\nThis is the standard \"render with Puppeteer, parse with Cheerio\" hybrid — but render is the exception, parse is always Cheerio.\n\n## (c) Per-domain corpus caching\n\n- **Key:** normalized registrable domain. **Store:** extracted page texts + the discovered URL tier-map + `ETag`/`Last-Modified` + fetch timestamp. **TTL:** 7-14 days for content, 24h for the URL map.\n- **Conditional fetches:** send `If-None-Match`/`If-Modified-Since`; a `304` means reuse cached text for ~0 cost. Big win for re-audits and for multi-tenant runs hitting the same popular domains.\n- **Robots/sitemap cache:** separate, longer TTL (24h) — never re-parse a 5,000-URL sitemap within a day.\n- **Storage:** compressed (brotli/gzip) text blobs on disk or a small KV (SQLite/Redis). 25 pages × ~30KB compressed ≈ <1MB/site; even 50k cached sites ≈ tens of GB — keep an LRU eviction cap (e.g. 2-5GB) so the tiny VM disk doesn't fill. In-memory LRU only for the hot set; cold sits on disk.\n- **Negative cache:** cache hard failures (DNS fail, 403, connection refused) for ~1h so a dead domain doesn't get retried every audit.\n\n## (d) Concurrency\n\nTwo independent dials:\n\n- **Audit-level (across sites):** **4-6 concurrent audits** on a 1GB VM, 8-10 on 2GB. This is the throughput multiplier: 5 slots × (one audit every ~90s worst case) easily clears 3,000/day. Bounded by a worker pool / semaphore, not unbounded `Promise.all`.\n- **Within a single audit (across that site's pages):** small pool of **4-6 parallel `got` fetches**, but **never more than 1-2 in-flight to the same domain** (politeness + WAF safety). 25 pages at 5-wide ≈ 5 batches ≈ a few seconds.\n- **Global headless semaphore:** **1** (the single shared browser). Non-negotiable on 1GB.\n\nTotal memory at peak: 6 audits × 6 static workers × ~10MB ≈ 360MB + one headless when active (~250MB) + Node/queue baseline. Fits 1GB with the headless gating; comfortable on 2GB.\n\n## (e) Async pipeline — crawl never blocks the mint-worker\n\nThe crawl/scan and the mint-worker must be **decoupled by a queue**, not in the same call stack. Pipeline:\n\n```\n[API: audit requested]\n      │  enqueue {auditId, url}  (returns immediately, 202 + auditId)\n      ▼\n┌─────────────────────────────────────────────────────────┐\n│  CRAWL QUEUE  (Redis/BullMQ or in-proc p-queue)          │\n│  concurrency = 4-6 (the audit-level dial)                │\n└─────────────────────────────────────────────────────────┘\n      │  each job, wrapped in 90s AbortController:\n      ▼\n  CRAWL WORKER (async, isolated)\n   1. cache lookup (domain) ──hit/304──► reuse corpus\n   2. fetch robots+sitemap → build tiered URL list\n   3. got-pool fetch ≤25 pages (Cheerio parse) \n        └─ SPA? → enqueue to HEADLESS sub-queue (sem=1)\n   4. assemble corpus → cache write\n   5. SCAN every word (the compliance rules engine)\n   6. write {auditId, findings, grade} to results store\n      │  enqueue MINT job  ◄── only the *result*, not the crawl\n      ▼\n┌─────────────────────────────────────────────────────────┐\n│  MINT QUEUE  (separate worker, separate concurrency)     │\n└─────────────────────────────────────────────────────────┘\n      ▼\n  MINT WORKER  — pulls finished results, mints. Pure & fast.\n   Never touches the network-bound crawl. Cannot be blocked\n   by a slow site because it only ever sees completed corpora.\n```\n\nKey properties that make it throughput-safe:\n\n- **Two separate queues / worker pools.** The mint-worker consumes only *finished* audit results. A site that hangs for its full 90s ties up one crawl slot, never the mint path. Mint stays fast and starvation-free.\n- **Everything `async/await` + bounded pool.** No synchronous fetch, no blocking parse on the main loop. Cheerio parse is sync-CPU but sub-ms at 2MB; if a pathological page ever blocks the event loop, the byte cap + a worker-thread offload for parse (optional) covers it.\n- **Per-job `AbortController` wired to every `got` request and the headless page.** The 90s site budget actually fires and frees the slot — caps are enforced by cancellation, not hope.\n- **Backpressure:** queue depth is the natural buffer. If ingestion spikes above 3k/day, jobs wait in Redis; workers drain at steady rate. The VM never thrashes because concurrency is fixed, not demand-driven.\n- **Idempotent + retried:** failed crawl → bounded retry (1-2x) with negative-cache; permanent failure still produces a gradeable result (\"could not fetch — graded on available signal\") so the mint-worker is never left waiting on a job that silently died.\n\n## Throughput proof\n\n5 crawl slots, average audit ~10-20s (cache misses, static path), worst-case capped at 90s.\n- Steady state at 15s avg: 5 / 15s = **0.33 audits/s = ~28,800/day** of headroom.\n- Even if every audit hit the 90s ceiling: 5 / 90s = **0.055/s = ~4,800/day** — still above the 3,000 target.\n\nThe 2-3k/day goal has **roughly 10x headroom** in the common case and stays above target even in the pathological all-timeout case. The bound (≤25 prioritized pages, static-first, single shared headless, two-queue async) is what creates that margin — not luck.\n\n---\n\n**TL;DR bound:** Don't crawl every page — crawl **≤25 pages chosen by compliance-priority** (home + sitemap-priority + all policy/legal pages + recent blog sample), depth ≤2, 90s/site, 2MB/page, static `got`+Cheerio by default, **one shared headless** as a <5% SPA-only fallback, per-domain conditional-GET cache, **4-6 concurrent audits**, and a **two-queue async pipeline** so the crawl can never block the mint-worker. Scan every word *of the pages you keep* — that preserves the \"every word\" accuracy promise where compliance signal actually lives, while keeping a 5,000-page site as cheap as a 20-page one.\n\nSources:\n- [Average Number of Pages on a Business Website (2026 Data) — Scalify](https://www.scalify.ai/blog/average-number-of-pages-business-website-2026-data)\n- [How Many Pages Should a Small Business Website Have — JPK Design Co](https://www.jpkdesignco.com/blog/how-many-pages-small-business-website)\n- [Google crawl file size limits (2MB HTML) — Nikki Pilkington](https://nikki-pilkington.com/google-updated-its-crawl-file-size-limits-and-you-probably-dont-need-to-care/)\n- [Crawl Budget — Ahrefs](https://ahrefs.com/seo/glossary/crawl-budget)\n- [Cheerio vs Puppeteer memory/throughput — iProyal](https://iproyal.com/blog/cheerio-vs-puppeteer/)\n- [Cheerio vs Puppeteer hybrid approach — Bright Data](https://brightdata.com/blog/web-data/cheerio-vs-puppeteer)"
  }
}