.env
.env.*
*.env
SECRET-KEYS.md
docs/API-KEYS-REGISTRY.md
node_modules/
client_email_files/
backups/
logs/
*.log
*.pem
*.key
uploads/
.DS_Store
INFRASTRUCTURE-INVENTORY.md
confirmations/
exports/
reports/
.mint-*.sql .remint-*.sql
db/seeds/*.upsert.sql

# runtime caches + local symlinks — never commit
.cache/
node_modules
.preflight_status

# golden-set raw payloads (shadow oracle) + identity map — never commit
eval/golden/.raw/
eval/golden/_identities.local.json

# Stryker sandbox + mutation report (regenerated every run)
.stryker-tmp/
reports/
